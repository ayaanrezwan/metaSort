# metaSort Project

### The absolute BEST sorting algorithm, topping all other standardized sorting algorithms

This is a prank, I made a sorting algorithm with O(n^n) time complexity to prank my friends.

Have some fun with the algorithm (try not to go too far above n = 9)